module synchronizer 
(
    input logic pulse,
    input logic clk,
    output logic synced
);

    always_ff @(posedge pulse, posedge synced) begin
        if(synced) pulse <= 1'b0;
        else pulse <= 1'b1;
    end

    always_ff @(posedge clk) begin
        synced <= pulse;
    end
endmodule